# Current State

> 只保存项目此刻的真实状态。历史结论进入 Decision、Evidence 或 archive。

更新时间：2026-07-22

## 当前里程碑

最小可测试规则切片完成，进入三种态度分支验证。

## 已完成且已验证

- 根目录 Git 仓库已建立，首次提交为 `7.22低保真原型`；
- React 版与单文件 HTML 均能完整演示首案；
- 收录证据后返回观察页，证据簿只读且没有行动入口；
- 观察页“证据簿”和“行动”可用时显示手型指针；
- `PlayerAction + Evidence + NPCState + seed → ActionResult` 的首个规则切片已实现；
- 专业指出矛盾会计算四项状态、阶段、原因日志，并按阈值触发 NPC 部分改口；
- React 版与单文件 HTML 的 P5 均读取规则输出，不再硬编码 after 数值；
- 构建、5 项自动测试与 ESLint 均通过；
- 390 × 844 浏览器实测无横向溢出，完整主路径无运行报错。
- 玩家/调试界面分离版已作为 Sites 第 8 版发布到现有老师演示地址。
- 玩家回应页与开发调试信息已分层：精确数值、seed、计算原因和系统进程移到桌面右侧栏，手机端自动隐藏。

## 已完成但尚未验证

- 当前规则参数与 Storylet 阈值是工作样例，尚未经过试玩与平衡验证；
- 温和/强硬仍以 UI 策略提示呈现，尚未进入规则结算；
- Unity、标准 Web/H5 与微信小游戏的最终工程路线尚未决定。

## 当前进行中

- 无；下一轮可直接扩展温和/强硬结算并做小规模试玩。

## 当前阻塞

- 无当前功能阻塞；技术路线决策等待分发入口澄清和最小技术验证。

## 最近获得的新证据

- 用户确认最终验收对象是游戏本体，优先级为玩法完整度 → 视觉效果 → 技术实现 → 文化内容；
- 用户确认现阶段需要精确 NPC 数值用于沟通，价格与交易数据暂不处理；
- 现有 Web 原型已证明浏览器路线能够承载当前 UI 与状态驱动闭环，但未证明它一定是最终最优引擎。
- 首个规则切片证明 UI 可只读取结构化 `ActionResult`，并由固定 seed 稳定复现结算与改口条件。
- 用户指出系统进程不应挤占玩家 UI；1280 × 900 桌面双栏与 390 × 844 手机单栏验证均通过。

## 与参考文档不一致之处

- `docs/06_UI_LOFI_TASK.md` 与 `docs/12_UI_LOFI_SPEC_DRAFT.md` 原先把 P5 数值定义为 Mock，现已更新为规则切片输出。
