# EXP-024：当前检查点与 `public` HTML 身份清单

日期：2026-08-11

## 为什么记录

用户明确暂不继续视觉精修，转向理解和打磨现行数值系统。本记录把已经完成的手机切片结构化停放，并核实 `prototype/public` 下五个 HTML 的身份，避免把冻结版本、历史实验或开发诊断面误当成当前玩家产品或生产数值权威。

本记录是仓库内可追踪检查点，不是 Git 提交或异盘备份。本轮没有修改五个 HTML 的字节，没有 stage、commit、push、merge 或部署。

## 当前恢复锚点

- 分支：`codex/v2-bootstrap`；检查时 `HEAD = b211e64132c1217bb6fc008cc9993710db8755c1`；
- 工作树：已有混合未提交改动，`git status --porcelain=v1 --untracked-files=all` 返回 66 条；本轮不整理或归一化其来源；
- 暂存区：0 个文件；
- 手机切片：实现、九张 `390×844` 截图、生成 HTML 哈希与独立 QA 已固化在 [EXP-023](EXP-023-first-mobile-human-object-slice.md)；
- 当前玩家 HTML：`317,224` bytes，SHA-256 `0A19A22F013AF714CBA407F147CA8243D1C83655D211609C7B80EF515AC8CC31`；
- 当前玩家 HTML、玩家源目录与 `generate-player.mjs` 仍是未跟踪文件，且尚无独立 V2 player release manifest；因此本记录能识别当前磁盘字节，但不能替代一个正式 Git 检查点；
- V1 canonical：SHA-256 `8B46D415627A2BDAA6D90C68C61189496DEAACCC1D3F1213ADC45A794464925B`，保持冻结。

## 五个 HTML 的真实身份

| 文件 | 当前身份 | 现在是否需要关注 | 证据与限制 |
|---|---|---|---|
| `掌眼_V2_玩家试玩版.html` | 当前唯一面向玩家的 V2 试玩入口 | **首要关注**。体验、三案例、玩家估值、报价与双轨复盘以它为准 | 由 `generate-player.mjs` 从当前玩家 React 与生产规则构建；内嵌三案、`rulesetVersion 2.0.0-alpha.1`、当前 `HEAD` 和 `sourceTreeStatus: dirty`；不含教师／开发表面 |
| `掌眼_V2_高保真演示.html` | V2 非公开开发／诊断运行时 | **技术上仍需关注，但不能当作当前玩家版**。适合 Agent 查看首案规则轨迹、精确状态和开发侧栏 | `release/v2-development.json` 指向它；当前文件内嵌来源提交 `7949cf1`、漆器 `caseVersion 1.0.0`，早于当前 `HEAD`，且只覆盖首案开发面。若要据它解释当前工作树，必须先单独重建和复核 |
| `掌眼_高保真教师演示.html` | V1 唯一 canonical 冻结演示 | **当前数值开发无需关注**；只在回归、老师旧演示、作品集对照或恢复时查看 | `release/v1-teacher-handoff.json` 唯一选中；字节哈希固定。禁止用 V2 构建覆盖 |
| `掌眼_低保真交互原型.html` | V1 历史低保真／调试快照 | **可停止关注**，除非回看设计演进或做历史回归 | 旧生成器被测试要求 fail-closed；测试只保护其自包含与历史身份，不要求复刻当前规则 |
| `掌眼_数值实验台.html` | V1 时期的隔离数值实验模型 | **不要把它当成当前数值系统**。仅在追溯旧假设时参考 | 模型和参数内嵌在 HTML，自身 VM 测试只证明内部确定性、归一化和分支可达；项目权威分类明确写为“隔离实验，不证明生产一致或平衡” |

## 文件校验

| 文件 | bytes | SHA-256 | `public` 与 `dist/client` |
|---|---:|---|---|
| `掌眼_V2_高保真演示.html` | 335,639 | `622C07504E6D66C2C3AC5E255180CC3DB0992D8238998F849852EE0DD0959501` | 一致 |
| `掌眼_V2_玩家试玩版.html` | 317,224 | `0A19A22F013AF714CBA407F147CA8243D1C83655D211609C7B80EF515AC8CC31` | 一致 |
| `掌眼_低保真交互原型.html` | 126,614 | `B93271C63FE97BD9EE7FA75B0550D2627B424ED991D1EB52B52E07BDCB94E0B7` | 一致 |
| `掌眼_高保真教师演示.html` | 326,768 | `8B46D415627A2BDAA6D90C68C61189496DEAACCC1D3F1213ADC45A794464925B` | 一致 |
| `掌眼_数值实验台.html` | 193,558 | `01CC4DDB3E1F6B5B6DAE7883A22D21DBD304E50351853A65BDE75C6EE9C65942` | 一致 |

`public` 与 `dist/client` 字节相同只证明复制完整，不证明文件仍代表当前生产规则、数值平衡或玩家体验。

## 对当前工作的直接结论

1. 用户平时只需打开 `掌眼_V2_玩家试玩版.html`；
2. Agent 梳理数值架构时应从 `game/`、`content/`、规则测试和回放入口读取生产权威，必要时把 `掌眼_V2_高保真演示.html` 当作旧一检查点的可解释界面；
3. 不应从现有 `掌眼_数值实验台.html` 反推当前正式参数。若后续需要“不会读代码也能调数值”的界面，应基于当前生产权威新建或重接诊断投影，而不是继续给旧实验台增加权威；
4. `掌眼_高保真教师演示.html` 与 `掌眼_低保真交互原型.html` 均保留但不进入当前决策链。

## 未验证与下一门

- 本盘点没有重新运行五个 HTML 的全部浏览器流程；其运行／视觉证据沿用各自已有记录；
- 尚未决定是否重建 V2 高保真诊断运行时，也尚未批准制作新的自然语言数值控制台；
- 若用户希望把当前未跟踪玩家版变成可从 Git 恢复的版本，需要另行批准精确 staging／commit 白名单；本轮没有把“保存”扩张成 Git 操作；
- 下一步先完成自然语言仓库解释工具的只读比较，再由用户决定采用“现有 Agent 直接生成中文系统说明”、轻量本地索引工具，还是更重的知识图谱／仓库 wiki。

## 本批收口复核

- 收口时分支仍为 `codex/v2-bootstrap`，`HEAD` 仍为 `b211e64132c1217bb6fc008cc9993710db8755c1`；
- 新增工具调研与数值地图后，`git status --porcelain=v1 --untracked-files=all` 为 70 条，暂存区仍为 0；条目增加来自本批项目记录，不代表产品代码又发生变化；
- 五个 `public` HTML 的大小与 SHA-256 已重算，全部与本记录表格一致；
- 项目记忆结构检查为 `0 errors / 0 warnings`，本批八个相关 Markdown 的本地链接检查通过；
- 本批最终仍未形成 Git commit、异盘备份或 release manifest，因此“进度已固化”准确含义是：当前字节身份、架构结论、工具研究和风险已写入工作树，可供继续工作；尚未获得 Git 级恢复能力。

## 后续状态：Git 快照已获批准

2026-08-11，用户随后明确批准提交全部未提交内容，并把提交命名为“研究数值系统前的快照”。因此本记录上方关于“尚未获得 Git 级恢复能力”的结论只描述批准前的初始盘点时刻，并由本次本地快照取代：

- 本记录、`EXP-025`、`EXP-026`、当前玩家 HTML、玩家源码与生成器均随同一提交收录；
- 提交前完整构建／自动回归为 `176/176 PASS`，玩家 HTML SHA-256 仍为 `0A19A22F013AF714CBA407F147CA8243D1C83655D211609C7B80EF515AC8CC31`；
- 该提交只建立本地 Git 恢复点，不构成异盘备份、release manifest、push、merge、部署或公开发布。
