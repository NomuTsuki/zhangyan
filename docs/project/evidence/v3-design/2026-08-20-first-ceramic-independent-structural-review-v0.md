# 第一器物独立结构复核 v0

Status: Evidence / Independent Structural Review Pass / Static-specification only

日期：2026-08-20

## 复核身份与结论边界

临时只读独立复核者在不修改文件、不暂存、不提交的条件下，交叉检查三档作者模型 v0.2、具体一局与四条换序、`OR-01—12`、DEC-021—023，以及 v0.2 明确继承的 v0.1 依赖折叠、轴路由、空间覆盖和行动前置段落。

**结论：Pass。** 当前没有规格级矛盾阻止项目冻结输入并进入确定性作者侧场景演算。这里的 Pass 只表示“规格足以成为下一步可执行夹具的输入”，不表示后验、换序、可达、成本、领域准确性、玩家理解、趣味、平衡、UI 或产品代码已经验证。

本轮复核时的主要输入哈希：

- [三档作者模型 v0.2](2026-08-17-first-ceramic-three-result-author-model-v0-2.md)：`073A10D52E8337361AE21DFBFB69256592CEDD2567EF5E188454E652DD023A00`；
- [具体一局与作者侧对抗复核 v0](2026-08-20-first-ceramic-concrete-session-and-owner-structural-review-v0.md)：`E4EA127106D5D57BF43E432FD71CF23D52E8251022E26B5B91964387A3991BFE`。

第二个哈希是 Minor 文案修正前的独立复核输入；修正后的复查哈希与结论将在本文“发现与处置”中追加，不覆盖这份来源证据。

## 发现与处置

### `IR-01` · Minor · 具体一局中的玩家投影批准状态已过时

具体一局仍把“自然语言首层、粗概率按需展开”的玩家投影写成未批准候选，并在 `OR-10` 中把整个方向停放；DEC-023 后来已经批准高层方向，只把具体概率组件、布局和文案保留为 `Prototype required`。

- 影响：只会让单独阅读该记录的人低估当前批准范围，不改变作者模型；
- 处置：在具体一局中标注 DEC-023 的后续批准，把高层方向改为 Active、具体组件继续 Unknown；
- 状态：Closed。Owner 已修正文案；同一只读复核者复查后给出 Pass，确认高层方向与组件边界准确、作者结构语义未改变。

### `IR-02` · Note · 确定性输入必须声明 v0.2 对 v0.1 的优先级

v0.2 只继承 v0.1 的依赖折叠、轴路由、空间覆盖和行动前置；v0.1 的八包与单锚点阶段解释已经被 DEC-022／v0.2 替代。若后续工具整体导入 v0.1，旧语义可能静默回流。

- 处置要求：场景演算先冻结一份输入清单，记录文件哈希、精确继承段落、v0.2 优先级和禁止继承表面；
- 状态：Required deterministic-stage input；不是当前规格阻断。

## 十二项复核矩阵

| # | 结论 | 独立复核观察 |
|---|---|---|
| 1 | Pass / static | 12 行、12 个唯一 ID、12 个唯一 `I × R × K` 组合；声明域为 `2 × 3 × 2`，固定真相只命中一包 |
| 2 | Pass by rule | G2 读取“老器且重大重组”的联合支持，不拼接两个边际；抽象分布可形成身份与重大重组边际均为 `0.76`、联合仅 `0.52` 的拒绝反例 |
| 3 | Pass by rule | v0.1 被继承段落定义稳定观察／来源／共享潜因 ID 和同源折叠；没有把多份同源材料当独立票 |
| 4 | Pass as contract only | 早拿后懂、只计一次、全量重算与换序不变合同相容；尚无求解器执行 |
| 5 | Pass by rule | 四类探测回报齐全；重复／同源／无区分力观察保留反馈与成本，但不加票 |
| 6 | Pass by rule | `no-signal / unresolved` 与 `absence-supported` 分离，并受方法、覆盖、灵敏度和材料条件约束；领域有效性尚未复核 |
| 7 | Pass by rule | 反证读取对象、区域、时间、材料层和量词；未命中的既有事实继续保留 |
| 8 | Pass / static coherence | G1—G3 证明骨架相容；G2 可承受无关表面／稳定性坏消息，但直接身份／重大重组反证可令其争议或回退；G3 允许具名非关键未知 |
| 9 | Pass by rule | 单份文档、单条胶线、单次 X 射线或单个仪器结果都不能独立建立 G2／G3；尚未执行答案键夹具 |
| 10 | Pass / static | 主路线明示为讲解路线，四条换序不设置阶段行动锁；硬前置只来自对象定位、可比时间点和物理可达 |
| 11 | Pass for claimed coverage | `OR-01—12` 唯一且状态诚实；未执行项仍标为 solver／numeric／player evidence absent |
| 12 | Pass with Minor | DEC-021 真相、DEC-022 三档、DEC-023 验证顺序和作者模型相容；只有 `IR-01` 的后续批准状态漂移 |

## 独立执行的检查

- 直接解析包 ID、三轴组合、蕴含计数、固定真相命中、`OR` ID 和路线拍点；
- 用抽象联合分布复现“两个边际较高、联合不足”的拼接反例；
- 重算两份输入 SHA-256；
- 检查工作树和 `git diff --check`；未把 owner 的 Conditional Pass 或 owner 静态检查当成独立验证结论；
- 复核前后没有文件或 Git 状态变化。

### 同一复核者修正后复查

- 修正后的具体一局 SHA-256：`5EC115C37DE3F1C56E3094AB86F1DD28AF32F40D69F5880361D5F8833291E46B`；
- 作者模型 SHA-256 保持 `073A10D52E8337361AE21DFBFB69256592CEDD2567EF5E188454E652DD023A00`；
- `IR-01`：Pass / Closed；
- `git diff --check`：Pass；
- 未观察到作者侧结构语义回归；复核者未修改文件或 Git 状态。

## 已验证与未验证

已验证的是：声明的 `2 × 3 × 2` 核心包在其定义域内静态互斥／穷尽；联合支持、依赖折叠、轴路由、重复不加票、阴性能力门、反证范围、阶段回退和 G3 允许未知的合同同时存在且没有发现规格级矛盾；具体一局没有静默恢复阶段行动锁。

仍未验证的是：实际后验与阈值跨越、G1—G3 可达、换序后完整状态相等、同源折叠／只计一次的执行结果、边际拼接／答案键／阴性／反证／回退／恢复夹具、替代路线非支配、成本与停手张力、似然与校准、领域准确性、玩家理解、趣味、UI、平衡和产品代码。

## 进入确定性演算前必须冻结的输入

1. 版本化输入清单：文件哈希、精确继承段落、v0.2 优先级与禁止回流表面；
2. 场景输入：固定 prior、证据多重集、稳定观察／来源／共享潜因 ID、轴权限、方法能力／覆盖、抽象作用域／量词、似然或确定性相容权重、阈值与证明规则；
3. 独立期望输出：G1—G3 可达、G2 收手／恢复、边际拼接拒绝、早拿证据、重复／同源、阴性未决、硬反证、合法 G2 降级、G3 保留未知和单项答案键拒绝；
4. 对同一证据多重集做合法换序的全量重算，比较完整后验、主张、冲突、覆盖与阶段，而不只比较最终徽章；
5. 保留原始输出与失败定位；反证夹具继续使用抽象作用域，不恢复已经撤回的具体举例。

成本、路线非支配与经济策略属于后续数值合同，不在本次 Pass 中偷跑。
