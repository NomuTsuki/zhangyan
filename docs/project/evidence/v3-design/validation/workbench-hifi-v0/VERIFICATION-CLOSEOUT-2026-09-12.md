# V3 阶段收尾验证

日期：2026-09-12。当前成果：[统一入口](../../../../delivery/index.html)。

## 改动与依据

用户已确认“先做试窗调查，再进行区域综合”，随后批准按建议做阶段收尾。本轮只收紧融合本地案例的综合取得条件，保留冻结求解器、事实、来源、计权、证明条件和预算；材料回看增加本局点位输入。已有道路、布局和动画参数无需改变。

视觉核验另发现：四项判断成立、没有显露的问题且取消选择后，右栏仍显示开局提示。本轮修正这处空状态文案，并增加真实浏览器断言；没有改判断结果或自动结束规则。

根 README、当前状态与下一步指向统一交付入口，旧说明明确保留为历史。整理16次演示路线、短复测、最新截图和原速短片。策划案、周报及冻结版本没有修改；本轮尚未提交或推送，基于已推送检查点 `14a54a2`。

## 最终构建

`prototype.html`：5,163,286 bytes；SHA-256 `72ff2c27a199729fce3678dbf2b1b7d32b9ecd6ea9df8eeb3b4860c6a3ac95a9`。

`build-stamp.json` 时间：2026-09-12T15:27:38.809Z。TypeScript 检查与 Vite 离线打包通过，47 modules transformed。Worker、图片、脚本和样式均内联；浏览器核验实际访问 `file://` 成品。

## 执行与结果

| 检查 | 实际结果 | 证明范围 |
|---|---|---|
| 新增表面收尾检查 | 6/6 | 缺前置不扣费、不预造结果；试窗解锁综合；输入回查；历史和重复；位置稳定；两条16次路线；22手段可达；冻结差异。 |
| 原融合会话检查 | 11/11 | 原材料、比较基底、延迟解释、重复与预算合同继续成立。 |
| 原作者冻结合同 | 40/40 | 冻结模型的既有测试与原始哈希校验继续通过。 |
| 原材料回看检查 | 6/6 | 所取得材料可读，只引用已取得来源。 |
| 原固定布局检查 | 8/8 | 固定位置、道路与标签的既有几何边界。 |
| 修改前浏览器复现 | 1/1 | 无点位记录仍可综合并扣一步，证实旧行为。 |
| 最终离线原生浏览器检查 | 5/5 | 缺前置按钮与原因；两次正常动效及历史；三个桌面尺寸的16次路线、20%缩放、拖动、恢复与收手。 |
| 交付入口浏览器检查 | 2/2 | 1280／1440宽度下无横向溢出，媒体元数据可读取，主按钮打开实际离线游戏。 |

入口页6个本地资源链接与新交付文档16个相对链接均存在；`git diff --check` 无输出、退出码0。最终成果页、整局截图与短片第4／10／16秒抽帧已人工查看，分别覆盖飞入、聚焦和接通后的画面。这是实现者的画面核验，不代替真人体验评价。

最终浏览器尺寸：1280×1000 DPR1、1440×1000 DPR2、1920×1000 DPR1，使用本机 Edge headless。三局均四项成立、G3、剩余6次，拖动未产生文字选择，历史回看保留当前总账，收手不重复扣费，无 pageerror 或 crash。

三局调查点击至下一帧最大分别为2.7／2.6／3.5ms，观察窗口内未记录长任务。这是有限输入响应观察，不是完整计算用时或对所有机器的性能承诺。全路线用减少动态效果隔离交互；表面两次调查另使用完整动效并录制原速视频。

完整动效实测依次经历 fly、focus、pullback、connect、idle；综合飞入时新节点 opacity 为0，到达后显露，原范围疑问消失并由道路接通。历史第一步只有点位，返回当前恢复两份材料。

## 原始证据与失败保留

本目录 `verification-closeout-2026-09-12/` 保存最终报告、逻辑检查输出及初始失败；详细浏览器观察和原始视频在仓库 `output/playwright/closeout-2026-09-12/`。

修改前新增逻辑检查为4/6：缺前置拦截和点位来源回查失败，其他既有路线继续成立。修订后6/6；没有修改旧测试或提高容差。原高保真 `check-contract.mjs` 含“整个 fusion 目录相对 HEAD 不变”的旧阶段范围断言，本轮明确改动 local-case，故不拿该断言作当前收尾门，也不改写它让它转绿。新检查以固定 `14a54a2` 和 V2 冻结 tag 验证应保护的目录，另覆盖实际路线、成本与历史。

## 完成证据报告

- Implemented：试窗取得前置、点位回查、右栏空状态修正，以及阶段成果入口、演示路线和精选资产。
- Executed：`npm.cmd run build`；`node --experimental-strip-types scripts/check-surface-closeout.mjs`；原 `local-session-check.mjs`、作者 `node --test contract.test.mjs`、`scripts/check-material-records.mjs`、`src/fixed-map-layout-check.mjs`；`scripts/check-closeout-browser.mjs --before` 和 `--tag=final`；交付链接和构建／媒体哈希读回。
- Scenarios covered：当前条件拒绝、合法两步、重复、全部22手段、两条16次路线、三尺寸浏览器、完整局部动效、20%缩放拖动恢复、历史、收手；成品入口本地跳转和媒体加载。
- Observed result：上述检查通过；页面与构建来源对应。短片18.56秒、1440×1000、25fps，保持原速。页面截图和短片抽帧已检查。
- Evidence provenance：本轮由实现者执行规则、真实浏览器与视觉检查，没有另行声称独立审查或真人验收。既有16次下界证明来自09-10独立核对；此次增加前置不会产生更短路线，两条16次路线实跑仍成立。
- Not verified：用户自己的最后一轮试玩、全部调查换序、不同设备／驱动下的偶发卡死、完整经济闭环、正式数值、导师验收与作品集最终排版。
- Residual risks：本机有界机器检查不能排除所有闪烁或冻结；原冻结案例仍保留旧的综合取得自由，当前批准差异集中在本地适配层；历史原型应按其原版本阅读。
- Exact completion claim supported：已完成可由机器核验的小范围收尾与本地交付整理，当前等待用户短复测；不宣称完整游戏或最终体验已验收。

`review required`：命中②新增检查文件，以及①收尾画面复查时额外修正右栏开局文案；未改冻结规则核或原测试／基线／容差。裁决留给用户转交的独立审查，本轮不自行裁决。
