# 核验报告与原记录的道路连续性

> 2026-09-13 后续反例：同一构建的[整图审查](../../2026-09-13-frontier-system-audit.md)未通过。先原记录、后核验、尚无整器观察时，旧归属／印证前沿仍会过早退出；材料等非来源分支也有相同机制。下面的历史通过结果原样保留，只支持其列明路径，不能作为所有顺序或整图连续性已修复的证据。当前产品尚未完成这次整图修正。

用户确认允许先取得核验结果、后取得原始记录；批准先提交已修好的英文版，再实现“虚线接通后退掉疑问，报告收纳时可看见去向”的呈现调整。

## 已保存的检查点

先完成本地提交 `2e5dafc`（完整 `2e5dafce87bbfd1cd36e1f72ce8bfda36fb4365f`），提交名 `Add bilingual workbench and repair order-dependent translations`。104个文件包括双语界面、换序漏译修复、对应验证与过程资产，以及该页面所依赖的已批准收尾基础和成果入口。未推送；临时输出、文档审查与周报资料留在工作区。检查点页面 SHA 为 `b0975f68be84929eb5c48558dc9e8aee54cb346cb18f21283d1a104d7de15ffb`。

暂存检查中，两份原始构建日志带有 Vite 输出的行尾空格。保留原始输出；排除这两份日志所在的原始文本日志目录后，其余暂存内容 `git diff --cached --check` 通过。没有为通过检查改写旧失败记录。

## 本次调整

当前页面 SHA：`370fc2c10025e8a5db358b2bcf1deaebd1a926c608220fe62c645b7545ebc842`，5,249,566 bytes，构建于2026-09-13T06:08:11.625Z。TypeScript与Vite构建通过，54 modules transformed。此次调整尚未再次提交／推送。

- 核验报告先到时，原记录疑问沿固定的对应路径伸出短虚线；不预造尚未取得的记录节点。
- 原记录后来到达时，复用原飞线、近看与拉回流程。对应实线从旧报告端沿同一条曲线逐段长向新记录。接通后旧问句退出，后续疑问再生长。
- 只有报告与原记录时，中性、无箭头的对应实线静态保留。点击它可回查这两份真实已取得材料，不把“对应来源”当作新的判断支持。
- 现器观察也取得、既有规则把报告并入主道路时，临时保留原报告与原路径；报告标记沿连接移入主道路，原独立表示再退场。主道路留下可点击的报告标记，材料页新增回查其原始记录的入口。
- 事故／后期记录的归属核验与实物印证四种报告均适用。两份报告可以共同等待同一原记录，各接各的来源线，不互相证明。
- 自动拉回取景同时包含即将退场的来源问句与随后出现的相关疑问；不再在接通时裁掉旧问句。近看阶段仍聚焦新材料。
- 拖缩、取消、历史、语言切换、收手、重开沿用现有中断机制，结束临时显示并直接呈现当前真实状态；减少动态效果时直接显示最终连接／标记。

实现位于高保真目录：新增 `src/archive-report-links.mjs` 提供只读来源对应投影，`engine.ts` 在冻结求解结果之后接入；布局和动效分别在 `map-layout.mjs`、`map-motion.mjs`、`MapView.tsx`，材料回查由 `MaterialViewer.tsx` 接入。来源线明确标记 `provenanceOnly`，不增加观察、事实、proof role或取得条件，也不传回求解器。没有改动融合版、旧原型、冻结作者文件或既有测试。

节奏和回退条件见[参数登记](../../../../PARAMS.md)的 `v3.hifi.archive-source.continuity`；它复用已有1800ms接通／移动和260ms间隔，属于Experimental，未宣称真人节奏验收。

## 执行与结果

以下命令在高保真目录运行。原始结果集中在 `verification-report-source-2026-09-13/`；它们的成品SHA与当前页面已核对一致。

| 检查 | 执行结果与覆盖 |
|---|---|
| `node --experimental-strip-types scripts/check-report-source.mjs` | 修复前5项失败；当前6/6通过。四种报告的缺口／实线同路径、问句退场时点、来源回查、无虚假判断支持；双报告同原记录；18次调查后仍可留4条来源线，补现器后4个标记收纳；最终节点／道路几何与另一合法顺序一致。 |
| `node scripts/check-report-source-browser.mjs --tag=second` | 6/6通过：1280／1440／1920宽度下四种报告逐段接通、原问句保留且不被拉回裁切、新问题延后、报告实际沿路径移动、标记与原记录可回查；现器先到时先接来源再收纳；缩放取消、历史、语言往返、20%缩放及重开。 |
| `node scripts/check-report-source-browser.mjs --tag=interrupt --only="Stopping, restarting"` | 1/1通过：报告移动过程中分别收手、重开、切换语言，临时报告／运动标记／临时道路清除，账本及最终图正确，等待后镜头不再被旧回调移动。 |
| `node scripts/check-contract.mjs` | 既有7/7通过：真实22动作、16步路线、受保护文件与既有规则保持；产生的 `verification-contract.json` 为本次运行输出。 |
| `node scripts/check-reveal-lifecycle.mjs` | 既有5/5通过，照片、表面等原接通时序未被本次分支破坏。 |
| `node src/fixed-map-layout-check.mjs` | 既有8/8通过：原固定骨架、照片／档案对应半路、表面前沿、有限换序、旧节点稳定与无相关遮挡。该旧检查主要使用融合原图；新增来源线另由本次检查及浏览器覆盖。 |
| `node --experimental-strip-types scripts/check-language-orders.mjs --tag=report-source` | 58条合法顺序、1,069个已知状态、391项文案，漏译0。 |
| `node scripts/check-language-branches.mjs --tag=report-source` | 既有9/9分支通过，898次文字扫描；疑问、道路、节点、材料及历史详情。 |
| `node scripts/check-language.mjs --tag=report-source` | 既有6/6通过，三种桌面宽度的16次调查／四项成立／余6次、22手段、两种比较、材料、历史、收手与个人原文、偏好和正常动效中切换。 |

实际浏览器为本机 Edge headless，直接打开离线成品；通过真实控件执行，快照仅用于读取，无会话或规则状态注入。正常动效测到报告在两次抽样之间确有44.7–189.1屏幕像素移动，来源实线的SVG进度持续变化，旧问句在接通期间保留。所有列出的完成运行未观察到pageerror或crash。

## 画面证据与保留的问题

[同两步对照、过程画面与原速片](../../../../../portfolio/2026-09-13-report-source-continuity/README.md)已保存。旧版对照从提交 `2e5dafc` 读取独立HTML，在临时文件上运行；未覆盖当前页。两版都实做“事故归属报告→事故原记录”，相同1440×1000视口／DPR2／全览命令；文字避让与取景因内容不同而变化。旧版0条来源线，新版1条。

第一轮动作检查通过后，查看实际截图发现接通时旧问句左侧被镜头裁切；对应截图保留为 `first`，随后把这条退场问句加入拉回取景并新增边界断言。`second` 为修订后的核验画面。早期新增模块有一次括号语法错误，修正后构建通过，原错误输出留存；没有通过删断言或放宽容差解决产品问题。

原速录屏另以 `--tag=capture --only="Already observed bowl"` 执行并通过，带真实前后停留，导出16.12秒、1440×1000、H.264 MP4。转码不改速，已用ffprobe核对格式并查看抽帧序列。此前11秒工作录屏也保留；9月12日原录像没有改动。

## 完成证据报告

- Implemented：已先提交英文版，再实现报告来源的固定路径、逐段接通、问句有序退场、沿路收纳和来源回查，补齐对应英文；项目记录与过程资产同步。
- Executed：上表命令、TypeScript／Vite构建、真实截图查看、录屏抽帧与ffprobe、页面和报告SHA核对、`git diff --check`。
- Scenarios covered：事故／后期、归属／实物印证四种报告；原记录先后与现器先后；两份／四份报告；正常／减少动效；取消、历史、切换、收手和重开；三种桌面宽度及原全流程双语回归。
- Observed result：新连接不再直接消失，旧问句等接通再退出；核验记录仍留存且可回查；收纳能观察到实际移动；列出的新旧检查均通过，没有观察到运行异常。
- Evidence provenance：实现者运行自动检查、查看实际截图与视频关键帧；没有独立审查或真人体验通过记录。
- Not verified：所有可能调查排列、其他浏览器／硬件；玩家是否能自然跟住此次接通／收纳，以及节奏是否适宜；未推送或部署。
- Residual risks：多份报告同次收纳时会并行动画，密集状态仍需真人评价；全览小字不等于逐项可读。新增来源对应属于呈现语义，未来新报告需明确绑定，不能按标题猜测。
- Exact completion claim supported：英文版已由 `2e5dafc` 本地保存；其后的来源道路调整已可运行并通过上述限定核验，留在工作区等待用户体验。

`review required`：②新增检查及镜头边界断言；③采用具体的来源曲线、报告标记与收纳节奏默认值。高保真 `engine.ts` 只改图投影入口，冻结会话与求解器未改；本执行者不作独立裁决。[短复测](PLAYTEST-REPORT-SOURCE.md)。
