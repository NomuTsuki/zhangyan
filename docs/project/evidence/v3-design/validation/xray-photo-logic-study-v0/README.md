# 旧照片缺哪一项？· 局部逻辑示意 v0

2026-09-07 · 可丢弃讨论稿 · **用户已评价“可以被理解了”，但指出这处汇合在当前前置约束下是冗余的，需修正。**

见[用户反馈与逻辑复核](PLAYTEST-RESULT-2026-09-07.md)。本页保留为当时的示意：其状态满足 `same ⇒ photo`。用户随后明确这次质疑不是调整指令，助手提出的“改为归属核验／接通”仍只是讨论观点。当前先[逐张展示现有游戏逻辑](../../2026-09-07-first-case-local-logic-diagrams.md)，不据此修改本页或整图。以下说明按当前未修改的 HTML 记录。

打开 [prototype.html](prototype.html) 即可离线使用，无需安装、构建或服务器。工作分支：`codex/xray-photo-local-study`；上一阶段检查点为 `d081c0b`。

本页只验证[局部候选](../../2026-09-07-xray-photo-local-logic-candidate.md)的一件事：能否从图中直接区分“当前实物拍到的信息可以使用”和“旧照片必须与同一对象核验共同成立”。它没有接入完整游戏，也没有修改旧冻结事件或求解器。

两种材料可以任选先后取得。旧照片出现后，尚缺的对象核验占据 AND 的另一条输入；使用明确标注的演示按钮补齐或撤回核验，图在原位接通／断开。X 射线单独提供当前信息，不依赖这次核验，也不参与旧照片的 AND。页面没有紫色解释线、黄色实心信息圆或共同问号节点。

**核验是显式模拟。** 现有作者资料只有分区结构、早期时点和稳定特征等接口，没有写定具体裂纹位置、旧照片画面或个体识别特征。本页没有用作者全局真相冒充一次调查的具体发现，没有伪造足以完成核验的图片。真实比对内容是后续工作；现在仅评价来源与共同条件的结构。

状态只保存在内存：`xray`、`photo`、`same` 三项。纯函数规则为 `now = xray`，`past = photo && same`；只有已取得照片才可设置 `same`。重来清空三项，撤回模拟核验保留两份材料。`studySnapshot()` 返回副本供核查。没有存档、费用或步数，也不产生重大重组等更远结论。

实际浏览器核验见 [VERIFICATION.md](VERIFICATION.md) 与 [browser-results.json](browser-results.json)，用户检查见 [PLAYTEST.md](PLAYTEST.md)。源代码为单 HTML，未新建正式测试套件；**review required** 保留于此 Experimental 呈现。过程资产见[前后对照](../../../../../portfolio/2026-09-07-xray-photo-logic-study/README.md)。

本页可读性尚未得到用户验证。先收集这一段的反馈，再决定是否补具体证据、制作核对过程，最后才扩展整张地图。
